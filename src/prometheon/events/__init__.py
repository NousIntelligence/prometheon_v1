"""Decentralized Validation event-stream layer.

Wire models, canonical bytes, and strict parsing for the four-family signed
event stream (``activity`` / ``identity`` / ``group`` / ``exclusion``) that
the BitFan platform pushes to validators.
"""
