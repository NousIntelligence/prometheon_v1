"""Mechanism interfaces and the Phase 1 weight engine.

This module is intentionally minimal. ``mechanisms.base`` defines the
abstract mechanism interface; ``mechanisms.phase1_growth`` implements Phase 1.

Future phases (2, 3, 4) will ship in their own repositories with their own
mechanism implementations. No scaffolding for future phases lives here.
"""
