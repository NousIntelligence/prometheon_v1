# Empty array

Positive canonical-JSON fixture. The strict parser MUST accept `input.json`; the JCS canonicalizer MUST produce exactly `canonical.json.txt` (UTF-8 bytes equal to `canonical.bytes.hex` decoded); the SHA-256 of those bytes MUST equal `canonical.sha256.hex`.
