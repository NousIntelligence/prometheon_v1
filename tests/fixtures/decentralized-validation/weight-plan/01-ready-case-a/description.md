# weight-plan 01-ready-case-a

Phase 1 ready WeightPlan with burn (Case A). `weight_plan_hash` is computed over the domain-prefixed JCS bytes and used by the validator for local-state debugging only; it never goes on-chain.
