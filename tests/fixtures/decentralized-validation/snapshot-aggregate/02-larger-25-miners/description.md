# snapshot-aggregate 02-larger-25-miners

Deterministic 25-miner aggregate snapshot. Miners are synthetic test SS58 addresses generated from `SHA-256('PROMETHEON-TEST-MINER-{i}')`. The fixture verifies that the canonicalizer + records_hash chain holds at non-trivial scale.
