# query-canonicalization 06-rfc3986-reserved-subdelims

Verify that `canonicalQueryString(params)` produces exactly the bytes in `canonical_query_string.txt`, and that `hashQueryString(params)` equals `query_hash.hex`.
