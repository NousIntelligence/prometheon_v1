"""Top-level pytest configuration.

Fixtures defined here are available to every test under ``tests/``. Category-
specific fixtures (unit, contract, integration) live in the matching
``tests/<category>/conftest.py``.

Markers are declared in ``pyproject.toml`` under ``[tool.pytest.ini_options]``.
"""

from __future__ import annotations

# No project-wide fixtures yet. Add them here as the implementation lands.
